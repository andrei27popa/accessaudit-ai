// Phase 2: Reports Module
// POST /scans/:scanId/reports (type: executive/technical)
// GET /reports/:id (download PDF)

import { Module } from '@nestjs/common';

@Module({})
export class ReportsModule {}
